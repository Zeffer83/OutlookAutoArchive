# Auto detect text files and perform LF normalization
* text=auto

# PowerShell scripts
*.ps1 text eol=crlf

# Documentation
*.md text diff=markdown
*.txt text

# Binary files (should not be modified)
*.exe binary
*.ico binary
*.dll binary
*.pdb binary

# Large files that should be tracked with Git LFS (if needed in future)
# *.exe filter=lfs diff=lfs merge=lfs -text
# *.ico filter=lfs diff=lfs merge=lfs -text

# Archive files
*.zip binary
*.tar.gz binary
*.rar binary

# Log files (should be ignored, but if tracked)
*.log text eol=crlf

# JSON files
*.json text eol=crlf

# YAML files
*.yml text eol=crlf
*.yaml text eol=crlf
